<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.shop.sample.product.repository.master.Sample2Dao">

    <!--
    	※ 경고
		이 select SQL은  Code Generator를 통하여 생성 되었습니다.
     	기본 쿼리 이고 수시로 변경 될 소지가 있기 떄문에 Generator로 변경 하는 것이 아닌 직접 수정은 하지 마십시요.
     	
    -->
    <select id="selectByPrimaryKey" parameterType="kr.co.shop.sample.product.model.master.Sample2" resultType="kr.co.shop.sample.product.model.master.Sample2">
		SELECT 
			 id, big_content, reg_dtime
		FROM 
			SAMPLE_2
		WHERE 
			 id = #{id, jdbcType=INTEGER} 
    </select>

</mapper>